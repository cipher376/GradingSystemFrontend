
export const API_PORT_LOCAL = '3000'
export const API_HOST_LOCAL = 'http://localhost'
export const API_URL_LOCAL = `${API_HOST_LOCAL}:${API_PORT_LOCAL}`




export const environment = {
  production: false,
  api_root_url: API_URL_LOCAL,
  file_api_root_url:'',
  file_api_download_url_root: '',
  file_api_upload_photo_video_url_root:'',
  file_api_upload_url_root:'',

};




/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
